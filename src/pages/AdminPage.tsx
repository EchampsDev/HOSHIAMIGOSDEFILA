import { Link } from 'react-router-dom'
import { Layout } from '../components/Layout'
export function AdminPage() { return <Layout><section className="content-card"><p className="eyebrow">ÁREA ADMINISTRATIVA</p><h1>Panel en preparación</h1><p>Esta ruta queda reservada para la moderación y organización del archivo. La autenticación y los permisos se conectarán en un módulo posterior.</p>{import.meta.env.DEV ? <Link to="/admin/experiencias" className="button secondary">Abrir centro administrativo</Link> : <p className="muted">No hay datos ni acciones administrativas disponibles todavía.</p>}</section></Layout> }
