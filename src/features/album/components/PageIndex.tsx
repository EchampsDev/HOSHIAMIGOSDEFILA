type Props = { open: boolean; pageCount: number; current: number; onClose: () => void; onGoTo: (page: number) => void }
export function PageIndex({ open, pageCount, current, onClose, onGoTo }: Props) { if (!open) return null; return <div className="album-index-backdrop" role="dialog" aria-modal="true" aria-label="Índice de páginas"><section className="album-index"><div><p>ÍNDICE</p><button type="button" onClick={onClose}>Cerrar ×</button></div><h2>Elige una hoja</h2><div className="album-index-grid">{Array.from({ length: pageCount }, (_, index) => <button type="button" key={index} className={current === index + 1 ? 'is-current' : ''} onClick={() => { onGoTo(index + 1); onClose() }}>{index + 1}</button>)}</div></section></div> }
