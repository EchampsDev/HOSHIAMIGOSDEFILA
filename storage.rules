rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // M1 does not upload assets. Future contribution uploads must be validated by a server-side flow.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
